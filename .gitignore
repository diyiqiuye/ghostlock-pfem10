# build output
*.o
a.out
/ghostlock
/exploit_guard
/model_check

# local run output
logs/
out/
*.log
*_evidence.txt

# ...but the curated evidence transcripts are tracked
!evidence/*.log
!evidence/**/*.log

# large device dumps (keep the repo light; regenerate from your own device)
kallsyms*.txt
syms.txt
kernel_payload.bin
config.txt
*.img
*.mbn

# vendor kernel modules — proprietary, never commit
oplus_*.ko
oplus_*.so

# root-solution binaries — not redistributed, take them from upstream (NOTICE.md)
kernelsu.ko
ksud
libkernelsu.so
